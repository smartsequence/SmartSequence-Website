---
import { BRAND_NAME } from '../consts';
import HeaderLink from './HeaderLink.astro';
import LanguageSwitcher from './LanguageSwitcher.astro';
import { getTranslations, getLocalizedPath } from '../i18n/utils';
import type { Language } from '../i18n/languages';

interface Props {
	lang: Language;
	currentPath: string;
}

const { lang, currentPath } = Astro.props;
const translations = await getTranslations(lang);

// 翻譯函式
function t(key: string): string {
	const keys = key.split('.');
	let value: any = translations;
	for (const k of keys) {
		value = value?.[k];
		if (value === undefined) return key;
	}
	return value || key;
}
---

<header>
	<nav class="container">
		<div class="logo">
			<a href={getLocalizedPath('/', lang)}>
				<span class="logo-text">{BRAND_NAME}</span>
			</a>
		</div>
		
		<div class="nav-links">
			<div class="nav-item-dropdown">
				<a href={getLocalizedPath('/product', lang)} class="nav-link">{t('nav.product')}</a>
				<div class="dropdown-menu">
					<a href={getLocalizedPath('/architecture', lang)}>
						<span class="dropdown-icon">⬡</span>
						<span>
							<strong>{t('common.learnArchitecture')}</strong>
							<small>{t('nav.architectureSub')}</small>
						</span>
					</a>
					<a href={getLocalizedPath('/compliance', lang)}>
						<span class="dropdown-icon">✓</span>
						<span>
							<strong>{t('common.learnCompliance')}</strong>
							<small>{t('nav.complianceSub')}</small>
						</span>
					</a>
				</div>
			</div>
			<HeaderLink href={getLocalizedPath('/pricing', lang)}>{t('nav.pricing')}</HeaderLink>
			<HeaderLink href={getLocalizedPath('/use-cases', lang)}>{t('nav.useCases')}</HeaderLink>
			<HeaderLink href={getLocalizedPath('/about', lang)}>{t('nav.about')}</HeaderLink>
			<HeaderLink href={getLocalizedPath('/contact', lang)}>{t('nav.contact')}</HeaderLink>
		</div>
		
		<div class="header-actions">
			<LanguageSwitcher currentLang={lang} currentPath={currentPath} />
			<button id="theme-toggle" class="theme-toggle" aria-label="切換深淺色模式">
				<svg class="sun-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
					<path stroke-linecap="round" stroke-linejoin="round" d="M12 3v2.25m6.364.386l-1.591 1.591M21 12h-2.25m-.386 6.364l-1.591-1.591M12 18.75V21m-4.773-4.227l-1.591 1.591M5.25 12H3m4.227-4.773L5.636 5.636M15.75 12a3.75 3.75 0 11-7.5 0 3.75 3.75 0 017.5 0z" />
				</svg>
				<svg class="moon-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
					<path stroke-linecap="round" stroke-linejoin="round" d="M21.752 15.002A9.718 9.718 0 0118 15.75c-5.385 0-9.75-4.365-9.75-9.75 0-1.33.266-2.597.748-3.752A9.753 9.753 0 003 11.25C3 16.635 7.365 21 12.75 21a9.753 9.753 0 009.002-5.998z" />
				</svg>
			</button>
			<a href={getLocalizedPath('/get-started', lang)} class="btn-try">{t('nav.tryNow')}</a>
			<a href={getLocalizedPath('/contact', lang)} class="btn-primary">{t('nav.requestDemo')}</a>
		</div>
	</nav>
</header>

<script>
	const themeToggle = document.getElementById('theme-toggle');
	
	if (themeToggle) {
		themeToggle.addEventListener('click', () => {
			if (typeof window.toggleTheme === 'function') {
				window.toggleTheme();
				// Update button icon state
				updateThemeIcon();
			}
		});
	}
	
	function updateThemeIcon() {
		const currentTheme = document.documentElement.getAttribute('data-theme') || 'dark';
		const themeToggle = document.getElementById('theme-toggle');
		if (themeToggle) {
			if (currentTheme === 'dark') {
				themeToggle.classList.remove('light');
				themeToggle.classList.add('dark');
			} else {
				themeToggle.classList.remove('dark');
				themeToggle.classList.add('light');
			}
		}
	}
	
	// Initialize icon state
	updateThemeIcon();
</script>

<style>
	header {
		position: fixed;
		top: 0;
		left: 0;
		right: 0;
		z-index: 1000;
		background: var(--bg-footer);
		backdrop-filter: blur(10px);
		box-shadow: var(--shadow-md);
		border-bottom: 1px solid var(--border-light);
	}

	.container {
		max-width: 1280px;
		margin: 0 auto;
		padding: 0 1.5rem;
	}

	nav {
		display: flex;
		align-items: center;
		justify-content: space-between;
		height: 5rem;
		gap: 2rem;
	}

	.logo a {
		text-decoration: none;
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}

	.logo-text {
		font-size: 1.125rem;
		font-weight: 700;
		color: white;
		white-space: nowrap;
		letter-spacing: -0.01em;
	}

	.nav-links {
		display: flex;
		align-items: center;
		gap: 2.5rem;
		flex: 1;
		justify-content: center;
	}

	.nav-links :global(a) {
		color: rgba(255, 255, 255, 0.9);
		font-weight: 500;
		font-size: 0.9375rem;
		transition: all 0.2s ease;
		text-decoration: none;
	}

	.nav-links :global(a:hover) {
		color: white;
	}

	/* Product dropdown */
	.nav-item-dropdown {
		position: relative;
	}

	.nav-link {
		color: rgba(255, 255, 255, 0.9);
		font-weight: 500;
		font-size: 0.9375rem;
		text-decoration: none;
		transition: color 0.2s ease;
		cursor: pointer;
	}

	.nav-link:hover {
		color: white;
	}

	.dropdown-menu {
		display: none;
		position: absolute;
		top: calc(100% + 0.75rem);
		left: 50%;
		transform: translateX(-50%);
		background: #1e293b;
		border: 1px solid rgba(59, 130, 246, 0.3);
		border-radius: 0.75rem;
		padding: 0.75rem;
		min-width: 280px;
		box-shadow: 0 16px 40px rgba(0, 0, 0, 0.4);
		z-index: 100;
		flex-direction: column;
		gap: 0.25rem;
	}

	.nav-item-dropdown:hover .dropdown-menu {
		display: flex;
	}

	.dropdown-menu a {
		display: flex;
		align-items: flex-start;
		gap: 0.75rem;
		padding: 0.75rem;
		border-radius: 0.5rem;
		text-decoration: none;
		color: rgba(255, 255, 255, 0.85);
		transition: background 0.2s ease;
	}

	.dropdown-menu a:hover {
		background: rgba(59, 130, 246, 0.15);
		color: white;
	}

	.dropdown-icon {
		font-size: 1.125rem;
		flex-shrink: 0;
		margin-top: 0.125rem;
		color: #3b82f6;
	}

	.dropdown-menu span {
		display: flex;
		flex-direction: column;
		gap: 0.2rem;
	}

	.dropdown-menu strong {
		font-size: 0.9rem;
		font-weight: 600;
	}

	.dropdown-menu small {
		font-size: 0.775rem;
		color: #94a3b8;
	}

	.header-actions {
		display: flex;
		align-items: center;
		gap: 1rem;
	}

	.theme-toggle {
		width: 2.5rem;
		height: 2.5rem;
		display: flex;
		align-items: center;
		justify-content: center;
		background: rgba(255, 255, 255, 0.1);
		border: 1px solid rgba(255, 255, 255, 0.2);
		border-radius: var(--radius-md);
		cursor: pointer;
		transition: all 0.2s ease;
		position: relative;
	}

	.theme-toggle:hover {
		background: rgba(255, 255, 255, 0.15);
		transform: translateY(-1px);
	}

	.theme-toggle svg {
		width: 1.25rem;
		height: 1.25rem;
		color: white;
		transition: all 0.3s ease;
		position: absolute;
	}

	.theme-toggle.dark .sun-icon {
		opacity: 0;
		transform: rotate(180deg) scale(0);
	}

	.theme-toggle.dark .moon-icon {
		opacity: 1;
		transform: rotate(0deg) scale(1);
	}

	.theme-toggle.light .sun-icon {
		opacity: 1;
		transform: rotate(0deg) scale(1);
	}

	.theme-toggle.light .moon-icon {
		opacity: 0;
		transform: rotate(-180deg) scale(0);
	}

	.btn-try {
		display: inline-block;
		padding: 0.75rem 1.25rem;
		background: transparent;
		color: var(--text-primary);
		border: 1px solid var(--border-color);
		border-radius: var(--radius-md);
		text-decoration: none;
		font-weight: 600;
		font-size: 0.875rem;
		transition: all 0.2s ease;
		white-space: nowrap;
	}

	.btn-try:hover {
		border-color: #10b981;
		color: #10b981;
	}

	.btn-primary {
		display: inline-block;
		padding: 0.75rem 1.75rem;
		background: var(--cta-primary);
		color: white;
		border-radius: var(--radius-md);
		text-decoration: none;
		font-weight: 600;
		font-size: 0.9375rem;
		transition: all 0.2s ease;
		white-space: nowrap;
		box-shadow: var(--shadow-sm);
	}

	.btn-primary:hover {
		background: var(--cta-hover);
		transform: translateY(-1px);
		box-shadow: var(--shadow-md);
	}

	/* 響應式設計 */
	@media (max-width: 768px) {
		nav {
			flex-wrap: wrap;
			height: auto;
			padding: 0.75rem 0;
		}

		.logo {
			flex: 1;
		}

		.nav-links {
			order: 3;
			flex-basis: 100%;
			justify-content: flex-start;
			gap: 1rem;
			margin-top: 0.75rem;
			padding-top: 0.75rem;
			border-top: 1px solid rgba(255, 255, 255, 0.1);
		}

		.header-actions {
			order: 2;
			gap: 0.75rem;
		}

		.btn-primary {
			padding: 0.5rem 1rem;
			font-size: 0.875rem;
		}

		.theme-toggle {
			width: 2.25rem;
			height: 2.25rem;
		}

		.theme-toggle svg {
			width: 1.125rem;
			height: 1.125rem;
		}
	}

	@media (max-width: 480px) {
		.nav-links {
			flex-wrap: wrap;
			gap: 0.75rem;
		}

		.logo-text {
			font-size: 1rem;
		}
	}
</style>
